{
  "name": "@manufactura/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"worker dev command will be added in next iteration\"",
    "build": "echo \"worker build command will be added in next iteration\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
