{
  "name": "@manufactura/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"web dev command will be added in next iteration\"",
    "build": "echo \"web build command will be added in next iteration\"",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
